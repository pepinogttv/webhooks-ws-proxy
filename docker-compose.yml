services:
  server:
    build: ./socket-proxy-server
    ports:
      - "3003:3003"
    environment:
      - API_SECRET=${API_SECRET}
      - PORT=${PORT:-3003}
    volumes:
      - endpoints-data:/app/data
    restart: unless-stopped

volumes:
  endpoints-data:

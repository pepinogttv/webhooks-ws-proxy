# Secret compartido entre server y client (REQUERIDO)
API_SECRET=tu-secreto-super-seguro-aqui

# URL del servidor (para el client)
SERVER_URL=https://tu-servidor-dokploy.com

# Puerto del servicio local al que se reenvian los webhooks
LOCAL_PORT=3000

# Puerto del server (para Dokploy)
PORT=3000
